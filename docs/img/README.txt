Drop four screenshots here so the User Manual shows them automatically:

  1-signin.png       sign-in screen
  2-hotasks.png      HO Tasks form
  3-storetasks.png   Store Tasks (Today's tasks)
  4-productquery.png Product Query feed

PNG or JPG both work (keep the .png name). No HTML editing needed -
the manual swaps the placeholder for your image as soon as the file exists.
